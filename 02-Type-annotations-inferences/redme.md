### When should a TypeScript developer prefer inference, and when should they explicitly annotate a type?

Answer: I prefer inference when the type is obvious from the value or context. I use explicit annotations when the type is not obvious, cannot be inferred, or when I want to clearly communicate an important contract.

